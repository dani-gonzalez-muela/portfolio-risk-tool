# Requires Python >= 3.9
# Uses `from __future__ import annotations` for Python 3.9 compatibility
# with modern type hint syntax (X | None). Python 3.10+ does not need this.

# Core dependencies
polars>=1.0.0
numpy>=1.24.0

# Development / testing
pytest>=8.0.0
pytest-cov>=5.0.0
